spring.application.name=iKong-server
# 기본 프로필 설정: dev(로컬 개발), prod(운영)
spring.profiles.active=dev

# 프로필별 설정 사용 (application-{profile}.properties 참고)
# - dev: H2 메모리 DB 사용 (로컬 개발용, Supabase 연결 불필요)
# - prod: Supabase PostgreSQL 사용